name: Apply Arcade Run playcheck V3 once

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/arcade-playcheck-v3-once.yml"
      - "scripts/apply-arcade-playcheck-v3.py"
      - "scripts/apply-arcade-playcheck-v3-fixes.py"

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: python3 scripts/apply-arcade-playcheck-v3.py
      - run: python3 scripts/apply-arcade-playcheck-v3-fixes.py
      - run: npm run test:rules
      - run: npm run lint
      - run: npm run build:pages
      - name: Commit V3 improvements
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add app/SkyDancerArcadeMode.tsx app/SkyDancerArcadeMode.module.css src/sky/arcade/SkyDancerArcadeCamera.ts src/sky/arcade/SkyDancerArcadeRuntime.ts src/sky/arcade/SkyDancerArcadeWebGLDemo.ts src/sky/arcade/SkyDancerArcadeProductPresentation.ts scripts/webgl-arcade-run-reference-audit.mjs tests/sky-arcade-run.test.ts
          git rm -f scripts/apply-arcade-playcheck-v3.py scripts/apply-arcade-playcheck-v3-fixes.py .github/workflows/arcade-playcheck-v3-once.yml
          git commit -m "Arcade Run playcheck V3 evasive combat polish"
          git push origin main
