name: Arcade V11.7 Loadout Identity Retry Once

on:
  push:
    branches: [main]
    paths:
      - .github/workflows/arcade-v117-loadout-identity-retry-once.yml

permissions:
  contents: write

jobs:
  implement:
    runs-on: ubuntu-24.04
    env:
      ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Apply V11.7 patch and migrate V11.6 source contract
        run: |
          python3 scripts/apply-arcade-v117-loadout-identity.py
          python3 - <<'PY'
          from pathlib import Path
          p = Path('tests/sky-arcade-run.test.ts')
          s = p.read_text()
          old = '  assert.match(menuSource, /MISSILE \\+22%/);'
          new = '  assert.match(menuSource, /id: "missile-focus"/);'
          if old not in s:
              raise SystemExit('legacy V11.6 menu contract anchor missing')
          p.write_text(s.replace(old, new, 1))
          PY
      - name: Rules regression
        run: npm run test:rules
      - name: Arcade contract
        run: npm run check:arcade
      - name: Lint
        run: npm run lint
      - name: Pages product build
        run: npm run build:pages
      - name: Commit product and cleanup
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/sky/arcade/SkyDancerArcadeRuntime.ts src/sky/arcade/SkyDancerArcadeWebGLDemo.ts app/CartGameMenu.tsx app/SkyDancerArcadeMode.tsx app/SkyDancerArcadeProduct.module.css tests/sky-arcade-run.test.ts
          git commit -m 'Arcade Run V11.7: differentiate loadout playstyles'
          git push origin HEAD:main
          git rm -f .github/workflows/arcade-v117-loadout-identity-once.yml .github/workflows/arcade-v117-loadout-identity-retry-once.yml scripts/apply-arcade-v117-loadout-identity.py
          git commit -m 'Remove V11.7 loadout identity work files'
          git push origin HEAD:main
