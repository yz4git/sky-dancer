name: Arcade Prism V8.9 Once

on:
  push:
    branches: ["main"]
    paths:
      - "scripts/apply-prism-v89.py"
      - ".github/workflows/arcade-prism-v89-once.yml"

permissions:
  contents: write

concurrency:
  group: arcade-prism-v89-once
  cancel-in-progress: true

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Apply V8.9 Prism reconstruction
        run: python3 scripts/apply-prism-v89.py
      - name: Rules regression
        run: npm run test:rules
      - name: Lint
        run: npm run lint
      - name: Pages production build
        run: npm run build:pages
      - name: Commit V8.9
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/sky/arcade/SkyDancerArcadeReferenceWorld.ts src/sky/arcade/SkyDancerArcadeModels.ts tests/sky-arcade-reference.test.ts
          git rm -f scripts/apply-prism-v89.py .github/workflows/arcade-prism-v89-once.yml
          git commit -m "Arcade Run V8.9: reconstruct Prism Citadel final assault"
          git push origin HEAD:main
