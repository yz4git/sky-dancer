name: Arcade Run V40 World Break Phase 2

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/arcade-run-v40-world-break-phase2-once.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.13.0
          cache: npm
      - name: Apply V40 World Break phase 2
        run: python3 scripts/arcade-run-v40-world-break-phase2.py
      - name: Install dependencies
        run: npm ci --no-audit --no-fund
      - name: Verify V40 World Break phase 2
        run: |
          npm run check:arcade
          node --import tsx --test tests/sky-arcade-v40-world-break.test.ts tests/sky-arcade-v40-world-break-phase2.test.ts
          npm run test:fast
          npm run lint
          npm run build:pages
      - name: Commit verified V40 phase 2
        run: |
          git config user.name "yz4git"
          git config user.email "65384484+yz4git@users.noreply.github.com"
          git add src/sky/arcade/SkyDancerArcadeRuntime.ts src/sky/arcade/SkyDancerArcadeWebGLDemo.ts src/sky/arcade/SkyDancerArcadeCanvasDemo.ts app/SkyDancerArcadeMode.tsx app/SkyDancerArcadeMode.module.css
          git commit -m "Arcade Run V40: add canyon and fleet objectives"
          git push origin HEAD:main
