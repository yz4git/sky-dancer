name: Arcade V10.4 Single Course Frame Once

on:
  push:
    paths:
      - '.github/workflows/arcade-v104-single-course-frame-once.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - name: Apply V10.4 single course frame patch
        run: python scripts/apply-v104-single-course-frame.py
      - name: Arcade rules regression
        run: npm run test:rules
      - name: Arcade typecheck
        run: npm run check:arcade
      - name: Lint
        run: npm run lint
      - name: Pages production build
        run: npm run build:pages
      - name: Commit product and cleanup temporary files
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/sky/arcade/SkyDancerArcadeCoursePath.ts src/sky/arcade/SkyDancerArcadeReferenceWorld.ts src/sky/arcade/SkyDancerArcadeWebGLDemo.ts tests/sky-arcade-reference.test.ts
          git commit -m "Arcade Run V10.4: unify background course frame"
          git push origin HEAD:main
          rm -f scripts/apply-v104-single-course-frame.py .github/workflows/arcade-v104-single-course-frame-once.yml
          git add -A
          git commit -m "Remove V10.4 single course frame work files"
          git push origin HEAD:main
